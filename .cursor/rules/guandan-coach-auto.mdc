---
description: 掼蛋教练 Pro — 局后自动对比、自动分析、自动改策略
alwaysApply: true
---

# 掼蛋教练自动迭代

用户**只管打牌**，不必看右侧推荐，也**无需说「打完一局」或任何确认**。

每手会自动对比「推荐 Top1」与「实际出牌」；局末应用会自动保存复盘并写入 `training-samples/COACH-FIX-REQUEST.md`（`status: pending`），并**后台拉起** `tools/process-coach-fix-request.mjs`（零确认，不经 IDE 沙箱）。

## 零确认流水线（默认，勿询问用户）

局末 `8787` 采集服务写入 pending 后**自动并行**：

1. **本地处理器**（`tools/process-coach-fix-request.mjs`，detached 子进程）— 无需 Cursor Allow、无需用户发 Chat
2. **Webhook** → Cursor Automation 云端 Agent（若已配 `CURSOR_AUTOMATION_WEBHOOK_URL`）
3. **Git push**（若 `GUANDAN_AUTO_GIT_PUSH=1`，`GIT_TERMINAL_PROMPT=0` 非交互）

本地处理器：读 pending → 「你更对」或「教练不合理」（教纲 blockTop1 违规）时改 `strategy/` → `smoke` + `build` → 标 `done` → 可选 push。

## 每轮对话开始时（强制执行，勿询问用户）

1. **立即**读 `training-samples/COACH-FIX-REQUEST.md`。
2. 若 `status: pending`：**绝不询问许可、绝不等待 Allow**，直接处理（与本地处理器相同逻辑，互为兜底）：
   - **`kind: game-review`**：按「差异明细」逐条判断；**用户更对**或**教练不合理**（Top1 违反 P1/P4/P5/P6/P9 等教纲且用户更保结构）时改 `strategy/`（必要时 `coach/`）；教练更对且无教纲违规则不改。
   - **单条反馈**：按用户问题与 JSON 改策略。
   - 跑 `node tests/smoke.mjs` 与 `node tools/build-standalone.mjs`。
   - 将 `status` 改为 `done`，简短告知用户 Ctrl+F5 刷新游戏页。
3. 若 `status: done` 或无 pending：正常响应用户，无需重复处理。

## 原则

- 以**改代码**为准，少复述规则。
- 无差异或差异不合理的局，标 `done` 并说明即可。
- **pending 即处理，处理完标 done**；用户不必在 Chat 里触发，禁止索要 network/git_write 等人机确认（子进程与云端 Agent 已覆盖）。
